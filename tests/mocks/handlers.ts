// Example API handlers - add your API mocks here
// Import http and HttpResponse from 'msw' when needed:
// import { http, HttpResponse } from 'msw'

export const handlers = [
  // Example: Mock GET request
  // http.get('/api/example', () => {
  //   return HttpResponse.json({ data: 'example' })
  // }),
]
